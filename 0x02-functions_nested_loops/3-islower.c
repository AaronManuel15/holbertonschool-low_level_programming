#include "main.h"
/**
 * _islower - Task 3 program that checks if the character is lowercase
 *
 * Return: 0 (Successful)
 *
 */
int _islower(int c)
{
	if (c <= 'z' && c >= 'a')
	{
		return (1);
	}
return (0);
}
