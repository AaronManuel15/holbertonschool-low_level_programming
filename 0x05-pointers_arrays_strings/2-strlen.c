#include "main.h"

/**
 * _strlen - Task 2
 *
 * @s: given from user to determine it's length
 *
 * Return: void
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
